#!/bin/sh

git diff --cached --name-only | xargs -L 1 -r autoformat
git diff --cached --name-only | xargs -r git add
NUM_FILES=$(git diff --cached --numstat | wc -l)
reset
echo "Formatted $NUM_FILES file(s)"
